```
https://docs.expo.dev/router/installation/#manual-installation
npx create-expo-app@latest
npx expo install expo-router react-native-safe-area-context react-native-screens expo-linking expo-constants expo-status-bar

# name of application app.json
schema: [name_app]

# start app -c (is to clean cache)
npx expo start -c

# create a component racourci
rnfes

# create layout racourci
rnfe

# hot reload press r

# Tailwind css (https://www.nativewind.dev/getting-started/expo-router)
npm install nativewind tailwindcss react-native-reanimated react-native-safe-area-context

# setup tailwind css
npx tailwindcss init

npx expo customize metro.config.js
```
